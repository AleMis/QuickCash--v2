<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="EmployeeKey">

    <resultMap id="result" type="EmployeeKey">
        <result property="employee_key_id" column="EMPLOYEE_KEY_ID"/>
        <result property="login" column="LOGIN"/>
        <result property="password" column="PASSWORD"/>
    </resultMap>

    <select id="selectLogin" resultMap="result">
        SELECT LOGIN FROM employeeskey WHERE LOGIN =#{login}
    </select>

    <select id="selectPassword" resultMap="result">
        SELECT PASSWORD FROM employeeskey WHERE PASSWORD =#{password}
    </select>

    <select id="selectEmployeeKey" resultMap="result">
        SELECT * FROM employeeskey WHERE  LOGIN =#{login}
    </select>

</mapper>
